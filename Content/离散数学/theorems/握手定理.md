---
title: 握手定理
type: theorem
tags: [离散数学, 图论]
date: 2026-04-24
aliases: [Handshaking Theorem, Handshaking Lemma, 握手引理]
related:
  - "[[离散数学/concepts/图论]]"
  - "[[离散数学/concepts/连通图]]"
  - "[[离散数学/concepts/有向图]]"
source_chapter: "Ch10"
source_book: "离散数学（Rosen）"
---

# 握手定理

> [!abstract] 概述
> ==握手定理==（Handshaking Theorem / Handshaking Lemma）：在无向图 $G = (V, E)$ 中，所有顶点的度数之和恰好等于边数的两倍，即 $\sum_{v \in V} \deg(v) = 2|E|$。

## 定理陈述

> [!def] 形式化陈述
> **定理**（握手定理）：
> 设 $G = (V, E)$ 为无向图（允许有重边和环），则
> $$\sum_{v \in V} \deg(v) = 2|E|$$
>
> **推论**：在任何无向图中，度数为奇数的顶点个数一定是**偶数**。
>
> **有向图版本**：在有向图 $G = (V, E)$ 中，
> $$\sum_{v \in V} \deg^-(v) = \sum_{v \in V} \deg^+(v) = |E|$$
> 即所有顶点的入度之和等于所有顶点的出度之和，且都等于边数。

## 证明概要

> [!info] 证明思路

### 无向图版本

**核心思想**：每条边恰好对度数之和贡献 $2$。

**详细证明**：

设 $G = (V, E)$ 为无向图。对每条边 $e = \{u, v\} \in E$：

- 边 $e$ 恰好连接两个端点 $u$ 和 $v$（若 $e$ 是连接 $u$ 到自身的环，则 $e$ 为 $u$ 的度数贡献 $2$）。
- 因此，每条边 $e$ 对 $\sum_{v \in V} \deg(v)$ 的贡献恰好为 $2$。

将所有边的贡献相加：

$$\sum_{v \in V} \deg(v) = \sum_{e \in E} 2 = 2|E|$$

证毕。$\blacksquare$

### 奇度数顶点个数为偶数的推论

设 $V_{\text{odd}}$ 为度数为奇数的顶点集合，$V_{\text{even}}$ 为度数为偶数的顶点集合。则：

$$\sum_{v \in V} \deg(v) = \sum_{v \in V_{\text{odd}}} \deg(v) + \sum_{v \in V_{\text{even}}} \deg(v) = 2|E|$$

因为 $2|E|$ 是偶数，且 $\sum_{v \in V_{\text{even}}} \deg(v)$ 是偶数（偶数之和仍为偶数），所以 $\sum_{v \in V_{\text{odd}}} \deg(v)$ 也必须是偶数。

但 $V_{\text{odd}}$ 中每个顶点的度数都是奇数，奇数个奇数之和为奇数，只有偶数个奇数之和才为偶数。因此 $|V_{\text{odd}}|$ 必须是偶数。$\blacksquare$

### 有向图版本

对有向图 $G = (V, E)$，每条有向边 $e = (u, v)$ 恰好：

- 为起点 $u$ 的**出度**贡献 $1$
- 为终点 $v$ 的**入度**贡献 $1$

因此：

$$\sum_{v \in V} \deg^+(v) = |E|, \quad \sum_{v \in V} \deg^-(v) = |E|$$

证毕。$\blacksquare$

## 关键推论

- **推论1（奇度数顶点为偶数个）**：如上所述，这是握手定理最常用的推论。可用于快速判断某个度数序列是否可能对应一个图。
- **推论2（度数序列可行性）**：一个非负整数序列 $(d_1, d_2, \ldots, d_n)$ 能成为某个图的度数序列的必要条件是 $\sum d_i$ 为偶数。
- **推论3（正则图的边数）**：$k$-正则图（每个顶点度数均为 $k$）若有 $n$ 个顶点，则边数为 $|E| = kn / 2$，因此 $kn$ 必须为偶数。

## 应用场景

1. **图的度数序列验证**：在判断一组度数是否能构成合法图时，握手定理提供必要条件。例如序列 $(3, 3, 3, 1)$ 的和为 $10$（偶数），而 $(3, 3, 3)$ 的和为 $9$（奇数），后者不可能。
2. **[[离散数学/concepts/连通图|连通图]] 的边数下界**：$n$ 个顶点的连通图至少有 $n - 1$ 条边（树），因为 $\sum \deg(v) \geq 2(n-1)$。
3. **社交网络分析**：握手定理的"握手"隐喻——在一次聚会中，与他人握手次数的总和是握手总次数的两倍（每次握手涉及两个人）。
4. **欧拉路径判定**：[[离散数学/concepts/图论|图论]] 中判断欧拉路径/回路的存在性时，需要分析奇度数顶点的个数（$0$ 个或 $2$ 个），握手定理保证了这个讨论是有意义的。

## 数值示例

考虑无向图 $G$：顶点集 $V = \{v_1, v_2, v_3, v_4\}$，边集 $E = \{\{v_1, v_2\}, \{v_1, v_3\}, \{v_2, v_3\}, \{v_3, v_4\}\}$。

度数：$\deg(v_1) = 2$，$\deg(v_2) = 2$，$\deg(v_3) = 3$，$\deg(v_4) = 1$。

验证：$\sum \deg(v) = 2 + 2 + 3 + 1 = 8 = 2 \times 4 = 2|E|$。$\checkmark$

奇度数顶点：$v_3$（度数 $3$）和 $v_4$（度数 $1$），共 $2$ 个（偶数）。$\checkmark$

## 参见

- [[离散数学/concepts/图论]]
- [[离散数学/concepts/连通图]]
- [[离散数学/concepts/有向图]]
- [[离散数学/concepts/完全图]]
- [[离散数学/concepts/二部图]]

## 参考链接

- [Handshake Lemma (U Colorado)](https://math.colorado.edu/~kstange/teaching-resources/discrete/handshake-lemma.pdf)
- [Graph Theory Notes (Cornell CS2802)](https://www.cs.cornell.edu/courses/cs2802/2019sp/2802graphtheory.pdf)
